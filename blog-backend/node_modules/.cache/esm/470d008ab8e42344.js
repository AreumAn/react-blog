let Koa,Router,bodyParser,mongoose;_b8e‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_b8e‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_b8e‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_b8e‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);require('dotenv').config();





const { PORT, MONGO_URI } = process.env;

mongoose.connect(MONGO_URI, {
    useNewUrlParser: true,
    useFindAndModify: false
}).then(() => {
    console.log('Connect to MongoDB');
}).catch(e => {
    _b8e‍.g.console.log(e);
});

const api = require('./api');

const app = new Koa();
const router = new Router();

// Set router
router.use('/api', api.routes());

// Apply bodyParser before router
app.use(bodyParser());

// Apply router to app instance
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(4000, () => {
    _b8e‍.g.console.log('Listening to port %d', port);
});
